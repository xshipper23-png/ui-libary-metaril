local Metaril = loadstring(game:HttpGet("DEINE_URL_HIER"))()

local Window = Metaril:CreateWindow({
    Name = "Metaril UI",
    LoadingTitle = "Metaril Framework",
    LoadingSubtitle = "by YourName"
})

local Tab = Window:CreateTab("Main")

Tab:CreateButton({
    Name = "Klick mich!",
    Callback = function()
        print("Button wurde gedrückt!")
    end
})
